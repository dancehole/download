# 需求与方案：相册 / 共享文件的「删除」与「过期」（2026-09-16）

## 一、需求原文

> 检查 download 项目里，相册和共享文件，删除是否能正确删除本地文件 + OSS 文件 + 其他东西，
> 完全释放内存。如果是过期，则只屏蔽链接，不删除本地文件（我发现文件的删除好像有问题）

拆成三条可验收的规则：

| # | 规则 | 说明 |
|---|------|------|
| R1 | **手动删除 = 清干净** | 相册「删除整个相册」/ 共享文件「删除」后，本地文件、OSS 对象、数据库记录（含级联的 photo、album_admin_acl）全部消失，不留孤儿 |
| R2 | **过期 = 只屏蔽链接** | 到达 `expires_at` 后链接失效（分享页 + 原图/预览/RAF/下载全部拒绝），**本地文件与 OSS 对象原样保留**，管理员可续期或手动清理 |
| R3 | **清理动作要可观测** | 删除过程中任何一端（本地 / OSS）没删掉，必须如实上报，不能静默 `pass` 导致空间悄悄泄漏 |

## 二、检查方法（都是真实跑出来的结论，不是读代码猜的）

新增三个可复用的巡检/回归脚本（放在 `tests/`）：

| 脚本 | 作用 |
|------|------|
| `tests/audit-storage.py` | 数据库 vs 磁盘：找孤儿文件、幽灵记录、路径失效 |
| `tests/audit-oss.py` | 数据库 vs OSS 桶：按 `{event_id}/` 前缀核对对象数与引用 |
| `tests/e2e-delete-expire.py` | 端到端回归：真调 8001 接口，造临时相册/共享文件，验证删除与过期行为，跑完自清理 |

### 巡检结果（改动前）

```
事件 3 条 / 照片 696 条 / 共享文件 1 条
[event 12 KX7BUB4P] 泽榆生日宴会   photo_count=145 磁盘文件=310 目录=有 oss_cleared=2026-09-16 16:15
[event 15 9EPBPDN8] 2023.旧约…    photo_count=196 磁盘文件=392 目录=有
[event 16 A2FNWGY2] 小学          photo_count=346 磁盘文件=692 目录=有

photo 原始图/预览图/RAF 缺失：0（数据库里的路径都还存在 ✅）
OSS：9EPBPDN8=196、A2FNWGY2=346，均与 photo 行一一对应，无孤儿 ✅
      KX7BUB4P 桶内 0 个对象（管理员已清空 OSS）✅
```

发现的历史残留（都是真孤儿，占用磁盘）：

```
153,552,717 /storage/files/3ypyzn7a72   ← 无任何数据库记录
     25,052 /storage/files/shr3r7rg2h   ← 无任何数据库记录
  6,930,903 /storage/KX7BUB4P/original/d39494904cd947c78ce4b62b6538f8cc.jpg  ← 无 photo 行引用
     56,052 /storage/KX7BUB4P/preview/dbbb5b7902274f8391568e61cebe39ab.jpg    ← 无 photo 行引用
share_file#1 auckn2ugtu：storage_path 指向旧目录 activity-image-list（项目改名后失效），文件已找不到
```

### 端到端实测（改动前，16/20 通过）

| 检查项 | 结果 |
|--------|------|
| 相册删除 → 本地目录消失 | ✅ |
| 相册删除 → OSS 对象清空 | ✅ |
| 相册删除 → event / photo / album_admin_acl 行清空（FK 级联有效） | ✅ |
| 共享文件删除 → 本地 + OSS + 数据库 | ✅ |
| **过期相册：分享页被拦截** | ❌ 返回 code=0，链接照常可看、原图照常能下 |
| **过期相册：原图下载被拦截** | ❌ HTTP 200（访问者能直接下载） |
| 过期相册：本地文件未被删除 | ✅ |
| **过期共享文件：分享页 info 被拦截** | ❌ 仅下载被拦，页面仍返回文件名/大小 |
| 过期共享文件：下载被拦截 + 本地文件保留 | ✅ |
| **storage_path 失效时删除是否清干净** | ❌ 记录删了、本地文件留下（孤儿成因） |

## 三、问题清单与成因

1. **过期相册完全不设防**（P0）：`routers/share.py` 只看 `local_cleared_at`，从不看 `expires_at`，
   到期后分享链接仍然可用。前端的注释也写着「仅过期的相册仍可正常浏览」——按新需求必须改成拦截。
2. **过期共享文件的分享页仍可打开**（P1）：`GET /share/files/{token}` 没有过期判断，只有下载端点判断。
3. **路径失效时删不干净**（P0，就是用户看到的「删除有问题」）：`_local_candidates`
   之前不存在——删除只试 `storage_path`；项目目录从 `activity-image-list` 改名成 `download` 后，
   老记录的绝对路径失效，于是「记录删了、文件留在 `storage/FILES_DIR/{file_id}`」→ 153MB 孤儿由此产生。
   下载端点本来有兜底逻辑，删除没有 → 两边行为不一致。
4. **静默失败**（P1）：共享文件删除时 OSS 异常被 `except: pass`；相册删除时 `clear_oss` 返回 -1
   也被忽略 → 数据库记录一删，OSS 对象永久失去索引，空间无法回收。
5. **清空 OSS 被误当成「相册已清理」**（P1，UI）：`purged = local_cleared OR oss_cleared OR purged_at`，
   后台用它把过期时间下拉框禁用、把卡片标成红色「已清理 · 照片已删除」。所以 KX7BUB4P 今天
   点了「清空 OSS」后，**过期时间不能再改**了（照片其实好好的）。照片上传区只按 `local_cleared` 隐藏，逻辑不一致。
6. **清空 OSS 在 OSS 关闭时会把引用抹掉**（P2）：`clear-oss` 端点在 OSS 未启用时仍会执行
   `clear_event_oss_keys`（把 `photo.oss_*` 置 NULL）+ 打标记，桶里的对象从此找不到引用。
7. **死代码**（P2）：`models.list_expired_share_files` / `mark_share_file_purged`（过期自动清理的遗留）
   已无调用方，且与「过期不删文件」的新策略冲突，容易误用。

## 四、本次改动

### 后端

| 文件 | 改动 |
|------|------|
| `app/routers/share.py` | 新增 `_block_reason(ev)`：`local_cleared_at` → 空壳提示；`expires_at <= now` → 「相册分享链接已过期」；5 个分享端点（info/photos/preview/original/raf）统一 `fail(410, …)`。**只拦截，不碰文件** |
| `app/routers/files.py` | 新增 `_local_candidates()`（`storage_path` + `FILES_DIR/{file_id}` 兜底）与 `_is_expired()`；删除改为逐个候选路径删除并如实上报；OSS key 存在即尝试删除，失败/未启用写入 `oss_error`；`share_file_info` 与 `download` 统一 410 拦截；下载复用同一套路径解析 |
| `app/cleanup_service.py` | 删除顺序固定为 **OSS → 本地 → 数据库**；OSS 删除失败则中止（不删数据库记录），返回 `success=False` + 原因；`rmtree` 后若目录仍在会告警；`clear_oss` 未启用时明确记日志 |
| `app/routers/events.py` | `clear-oss`：若相册仍有 OSS 引用但 OSS 未启用 → `fail(503)` 中止（不抹掉引用）；`DELETE /events/{id}`：`success=False` → `fail(502, 原因)` |
| `app/models.py` | 新增 `count_event_oss_photos()`、`heal_share_file_paths()`（启动自愈）；删除无调用的 `list_expired_share_files` / `mark_share_file_purged`（保留 `purged_at` 列以兼容历史数据） |
| `app/main.py` | 启动时调用 `heal_share_file_paths(FILES_DIR)`，修正 share_file 里失效的 `storage_path`（项目改名遗留），失败不影响启动 |
| `app/response.py` | 新增 `fail_http(code, msg)`：业务失败 + **真实 HTTP 状态码**。公开分享端点（原图/预览/下载会被浏览器直接打开）改用 410，避免 `window.open` 把 `{"code":410}` 当文件下载、图片位变碎图（管理端 API 仍走 `fail()` 的 HTTP 200 + body.code 约定） |

### 前端

| 文件 | 改动 |
|------|------|
| `js/admin.js` | 卡片/详情状态改用 `local_cleared` 判定「已清理」（红），`oss_cleared` 单独显示灰色「OSS 已清空」；过期时间下拉框的 `disabled` 改按 `local_cleared` → 清空 OSS 后仍可改过期时间；共享文件删除返回 `success=false` 时弹出「仍有文件未清干净」的告警 |
| `file.html` | 失效卡片显示后端返回的具体原因（如「链接已过期」）；「已清理」文案不再写「自动删除」；内联 i18n/api 版本号 +1 |
| `js/gallery.js` | 注释与新行为对齐（过期 = 拦截链接） |
| `js/i18n.js`、`admin.html`、`gallery.html` | 新增 `delete_file_partial`、`cleaned_oss_chip` 文案；静态资源 `?v=` 版本号 +1 以确保浏览器不拿旧脚本 |

验证方式：`cd backend && venv/bin/python ../tests/e2e-delete-expire.py`（20/20 通过后再上线）。

## 五、待确认 / 未执行事项

1. ~~需要重启服务~~ → **已于 2026-09-16 16:42 上线**（见第六节）。
2. **磁盘上的历史孤儿**（153MB + 25B + 7MB + 56KB，共约 160MB）是否清理？
   清理脚本会先打印清单、逐个 `os.remove`，不动任何数据库记录。**仍未执行**，等确认。
3. **即将到期的两个相册**：9EPBPDN8 与 KX7BUB4P 都是 **2026-09-19** 到期，
   改动上线后链接会自动失效（本地文件保留）。是否需要顺延过期时间？**等确认。**
4. **共享文件过期后的分享页形态**：本次统一为「链接失效」（显示后端给的过期原因），
   与相册一致——已按此形态上线。若你更想保留「显示文件名+大小、但禁用下载按钮」的老形态，改一行即可。
5. ~~`share_file.storage_path` 启动自愈~~ → **已实现并上线**（`models.heal_share_file_paths()`
   + `main.py` lifespan 调用），上线时已修好 share_file#1 指向旧目录 `activity-image-list` 的记录。

## 六、实施结果（2026-09-16 上线）

- commit：`3d7d6e5`（主体改动）+ 本次 e2e 修复的 `fail_http` 补丁
- 上线动作：`systemctl restart download`，`/download/api/health` 本地 + 公网均 200
- 启动自愈实测：`share_file#1 auckn2ugtu` 的 storage_path 由
  `/home/dancehole/project/activity-image-list/backend/storage/files/auckn2ugtu`
  自动改回 `.../download/backend/storage/files/auckn2ugtu`（文件确实在，路径已可解析）

### 验收结果

| 项 | 结果 |
|----|------|
| `tests/e2e-delete-expire.py` | **20/20 通过**（首轮 19/20，唯一 FAIL 是断言写死了 HTTP 状态码，而 `fail()` 走的是项目「HTTP200 + body.code」约定 → 补 `fail_http()` 给公开分享端点真实 410 后全绿） |
| 线上真实相册防回归 `tests/regress-live-share.py` | KX7BUB4P / 9EPBPDN8 / A2FNWGY2 三个相册 info 均 `code=0`，原图 http200 `image/jpeg`（6.9MB / 3.9MB / 51KB）、预览 http200 → **未误伤未过期链接** |
| 线上真实过期共享文件 `share_file#1`（2026-07-31 到期） | info / download 均 **HTTP 410** + 中文原因，本地文件仍在 ✅（正是「只屏蔽链接、不删文件」） |

### 零改动声明

数据表结构未改（只复用既有 `purged_at` / `local_cleared_at` / `oss_cleared_at` 列）；
未删除任何线上照片或共享文件本体；未改 Nginx 配置。
